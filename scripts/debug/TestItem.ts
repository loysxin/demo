import { _decorator, Component, director, find, Game, game, Graphics, profiler, Node, Camera, View, UITransform } from 'cc';

const { ccclass, property } = _decorator;

@ccclass('TestItem')
export class TestItem extends Component {
    protected onLoad(): void {
        
    }
    protected start(): void {
        
    }
}